module.exports = {
  theme: {
    extend: {
      colors: {
        primary: 'var(--primary-color)',
      },
      backgroundColor: {
        base: 'var(--background)',
        card: 'var(--card-bg)',
      },
      textColor: {
        base: 'var(--foreground)',
      },
      borderColor: {
        default: 'var(--border-color)',
      }
    }
  }
}